{
  "dependencies": {
    "gh-pages": "^6.2.0",
    "three": "^0.171.0"
  },
  "devDependencies": {
    "vite": "^6.0.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  }
}
